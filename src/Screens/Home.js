import React from 'react';

const Home = (props) => {
  return (
    <div>
      <h1 style={style.header}>Home Page</h1>
    </div>
  );
};

const style = {
  header: {
    color: 'red',
    fontSize: '18px',
  },
};

export default Home;
